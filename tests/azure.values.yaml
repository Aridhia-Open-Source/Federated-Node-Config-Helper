db:
  user: admin
  host: db.default.svc
  secret:
    key: password
    name: local-db

host: someurl.com
cert-manager:
  enabled: true
  namespace: certmanager

outboundMode: true

traefik:
  enabled: false

certs:
  frequency: 65
  azure:
    secret: test-azure-qc
    configmap: test-azure-qc
storage:
  azure:
    secretName: azstorage
    shareName: files
on_aks: true
