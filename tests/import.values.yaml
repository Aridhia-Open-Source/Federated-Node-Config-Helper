db:
  user: admin
  host: db.default.svc
  secret:
    key: password
    name: local-db

host: someurl.com
cert-manager:
  enabled: true
  namespace: certmanager

outboundMode: true

traefik:
  enabled: false

firstUserSecret:
  name: first-user
  passKey: pass
  firstName: Jack
  lastName: Black
  email: jackblack@email.com
